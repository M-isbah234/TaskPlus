# TaskPlus
TaskPlus is a lightweight task management API built with FastAPI and SQLAlchemy, featuring a clean web interface for tracking tasks with priorities, due dates, and status updates (pending, in-progress, completed). It uses a local SQLite database for simple, self-contained task organization.
